"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractId = extractId;

// extract a story id
function extractId(_ref) {
  var id = _ref.id,
      name = _ref.name;

  if (id) {
    return id;
  }

  return name.replace(/\W+(.)/g, function (_, chr) {
    return chr.toUpperCase();
  });
}